#include "monte_carlo.h"

node* create_node(position* pos){
  node* res = malloc(sizeof(node));
  res->pos = pos;
  res->nb_visits = 0;
  res->win_rate = 0;
  res->parent = NULL;
  for(int i = 0; i < 6; i++){
    res->children[i] = NULL;
  }
  return res;
}

node* policy_tree(node* tree){
  
}
